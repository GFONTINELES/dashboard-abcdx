streamlit run consulta_abcdx_estoque.py para todas as lojas
copia e cola todas as alterações do OneDrive para a pasta local
versionamento das mudanças para o git
atualiza o site(deploy)

como gerar senha nova:

import streamlit_authenticator as stauth

hashed_passwords = stauth.utilities.hasher.Hasher.hash_passwords({
    "usernames": {
        "novo_gestor": {
            "password": "gestor@123"
        }
    }
})

print(hashed_passwords)

